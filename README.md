# cashew-classifier
A machine learning web app that classifies cashew nut grades (DARK_BROWN, HALF_UPW, REJECTION, TESTA) using a MobileNetV2-based CNN model, built with TensorFlow and deployed via Streamlit. Includes Firebase Firestore integration for logging predictions.
